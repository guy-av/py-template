[project]
name = "plat-image"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guy Avital", email = "guyavital10@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "attrs>=25.3.0",
    "plat-common",
    "returns>=0.26.0",
]

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "syrupy>=4.9.1",
]

[tool.uv.sources]
plat-common = { workspace = true }
